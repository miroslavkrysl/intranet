{% extends "base/wide-message.twig" %}

{% set heading = 'Whoops!' %}

{% block message %}
    <p>
        {{ text }}
    </p>
    <p>
        {{ _text('base.webmaster')|capitalize }}: <a href="mailto:{{ _config('app.webmaster') }}">{{ _config('app.webmaster') }}</a>
    </p>
{% endblock %}
